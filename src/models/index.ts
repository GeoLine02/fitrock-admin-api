import sequelize from "../config/database";

// Import models below as you create them
// Example:
// import User from './User';

// Initialize models
// Example:
// User.initModel(sequelize);

// Associate models (if relationships exist)
// Example:
// User.associate();

export { sequelize };
